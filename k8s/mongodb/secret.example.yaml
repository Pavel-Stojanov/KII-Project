# Template. Copy to secret.yaml (gitignored), fill in real values, then run k8s/seal-secrets.sh.
apiVersion: v1
kind: Secret
metadata:
  name: mongodb-secret
  namespace: library-api
type: Opaque
stringData:
  MONGO_INITDB_ROOT_USERNAME: "REPLACE_ME"
  MONGO_INITDB_ROOT_PASSWORD: "REPLACE_ME"

# Template. The real keyfile.secret.yaml (gitignored) is generated by k8s/seal-secrets.sh
# via `openssl rand -base64 756`. This template documents the shape only.
apiVersion: v1
kind: Secret
metadata:
  name: mongodb-keyfile
  namespace: library-api
type: Opaque
stringData:
  # Shared secret for replica-set member authentication. Generate with:
  #   openssl rand -base64 756 | tr -d '\n'
  keyfile: "REPLACE_ME"

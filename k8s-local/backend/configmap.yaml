apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: library-api
data:
  SPRING_APPLICATION_NAME: library-api
  # Local: the app is reached at http://localhost via the nginx ingress.
  CORS_ALLOWED_ORIGINS: "http://localhost"
